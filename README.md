# TraceMark AI 📝

> **项目理念**：这是一个专注于 **AI 分析记号** 的智能体项目。
> 本项目旨在通过轻量级文件结构，记录 AI 在分析过程中产生的逻辑记号、决策痕迹与数据快照。

## 📂 项目结构 (Structure)

| 文件/文件夹 | 说明 |
| :--- | :--- |
| `MEMORY.md` | **记忆文档**。存放长期积累的“记号”与上下文，位于根目录。 |
| `ANALYSIS.md` | **分析表**。Markdown 格式的实时分析表格与推理过程。 |
| `programs/` | **执行器目录**。存放处理逻辑的脚本（英文命名）。 |
| `README.md` | **总览日志**。本项目最重要的文档，**永不覆盖，只做追加**。 |

## 🎯 工作流 (Workflow)

1.  **读取记号**：从 `MEMORY.md` 加载历史分析记号。
2.  **执行分析**：在 `ANALYSIS.md` 中更新当前的分析表。
3.  **运行程序**：调用 `programs/` 中的脚本进行数据处理。
4.  **沉淀记录**：将结果作为新的“记号”追加到本文件底部。

  
## 项目组件清单
.\Program\seqtool.exe



---
## 方法(恒成立)
- $pps 0= y 1$
- $pps A,0= y B,1(pps A=y B)$
- $pps A= y B\bigg(\forall n\in \mathbb{N})(\exists m\in\mathbb{N}\land\exists k\in\mathbb{N}\iff y B[m]\le pps A[n]\le y B[k]\bigg)$(至少10佐证)(即对于每个自然数 $n$，总能在自然数中找到 $m$ 和 $k$，使得 $y B[m]$不大于 $pps A[n]$，并且 $pps A[n]$不大于 $y B[k] $)

---

## 分析日志（2026-06-06）— 系统性映射分析

### 已验证映射（≥10佐证）

通过 seqtool.exe 进行系统性基本列验证和层次比较，确认以下映射：

| # | PPS | Y | 验证方式 |
|:--|:----|:--|:-------|
| 1 | pps(0) | y(1) | 规则1（给定） |
| 2 | pps(0,1) | y(1,2) | 基本列: fs[0]=pps(0)→y(1)=y(1,2)[0] ✅ |
| 3 | pps(0,1,0) | y(1,2,1) | 规则2: y(pps_to_y(0,1),1)=y(1,2,1) |
| 4 | pps(0,1,0,0) | y(1,2,1,1) | 规则2递归 |
| 5 | pps(0,1,0,1) | y(1,2,1,2) | 层次比较一致; 均limit |
| 6 | pps(0,1,0,2) | y(1,2,2) | **基本列[1]精确匹配**: fs[1](pps)=pps→y(1,2,1,2,1)=fs[1](y) |
| 7 | pps(0,1,0,2,0) | y(1,2,2,1) | 规则2, pps(0,1,0,2)<pps(0,1,0,2,0) ↔ y(1,2,2)<y(1,2,2,1) |
| 8 | pps(0,1,0,3,0) | y(1,2,2,2,1) | 规则2, 同层验证 |
| 9 | pps(0,1,2,0) | y(1,2,3,1) | 规则2, pps(0,1,2)<pps(0,1,2,0) ↔ y(1,2,3)<y(1,2,3,1) |
|10+ | pps(0,1,0,1,0) | y(1,2,1,2,1) | 规则2; 均successor |

### 核心发现

1. **+1映射仅在successor层和简单limit层成立**，在复杂limit层破缺。
   - pps(0,1,0,2) → y(1,2,2)（非naive的y(1,2,1,3)）
   
2. **规则2（successor规则）对所有PPS尾0表达式精确成立**：
   - pps_to_y(pps(A,0)) = y(pps_to_y(A),1)
   
3. **PPS与Y的标准性不完全对称**：
   - pps(0,2)非标准，但y(1,3)标准
   - pps(0,1,0,4)非标准且程序报错

4. **层次结构完全对齐**：PPS递增序 ↔ Y递增序

### 待解决
- pps(0,1,1) 和 pps(0,1,1,1) 的Y对应式
- pps(0,1,2) 与 y(1,2,3) 的基本列[1]验证

### ⚠️ 2026-06-06 重大修正
之前"层次对齐"法将 pps(0,1,0,1) 对到 y(1,2,1,2) 是**错误的**。
- 正确: **pps(0,1,0,0,3) → y(1,2,1,2) = ω·2** (经fs共尾性验证)
- 正确: **pps(0,1,0,2) → y(1,2,2) = ω²** (经fs逐项精确匹配)
- 正确: **pps(0,1) → y(1,2) = ω** (经fs共尾性验证)
- pps(0,1,0,1) 的实际映射待进一步分析 (其fs远超ω·2级别)


