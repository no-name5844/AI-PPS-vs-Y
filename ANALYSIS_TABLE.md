# TraceMark 分析表

## 分析原则（2026-05-04 确立）

1. **从小到大**：从最小、最简单的案例开始分析
2. **依赖已有规律**：分析大案例时，依靠前面已发现的规律
3. **谨慎推断**：不符合已知模式的现象，不轻易下结论
4. **追求详细**：深入细致地分析，而非泛泛而谈
5. **敢于推翻**：如果发现已有的假设或理论与实际结果不符，及时修正或重新考虑或重构

***

## 核心规则

| 规则 | 内容 |
|:---|:---|
| R1 基点 | `pps 0 = y 1` |
| R2 后继 | 若 `pps A = y B`，则 `pps A,0 = y B,1` |
| R3 夹逼 | 极限对应需满足对任意 n，存在 m,k 使得 yB[m] ≤ ppsA[n] ≤ yB[k] |

验证工具：`seqtool.exe PPS/Y expand -s <seq> -n <count>`

***

## 基础对应（从小到大）

| y | pps | 验证 |
|:---|:---|:---|
| 1 | 0 | R1 |
| 1,2 | 0,1 | 展开匹配（ω） |
| 1,2,1 | 0,1,0 | R2 |
| 1,2,2 | 0,1,0,2 | 展开：交替重复 |
| 1,2,3 | 0,1,2 | 展开：常数尾缀（修正：非 0,1,0,3） |
| 1,2,1,1 | 0,1,0,0 | R2 |
| 1,2,1,2 | 0,1,0,0,3 | 展开：前缀+无限0尾缀 |
| 1,3 | 0,1,2,3 | FS位置+R2 |
| 1,2,4 | 0,1,1 | FS位置+R2 |
| 1,2,2,2 | 0,1,0,0,3,3 | R2链 |
| 1,2,3,3 | 0,1,2,2 | R2链 |
| 1,3,2 | 0,1,2,3,2 | R2链 |
| 1,3,3 | 0,1,2,3,3 | R2链 |

---

# 通用规律（7支）

规律一旦成立，可延至任意深度。以下为归纳总结，不再逐项展开。

## 1. 交替分支

`y (1,2)^n = pps 0,1,0,0,3,0,0,6,...,3(n-1)`

- Y 元素数 = 2n，PPS 元素数 = 3n−1
- PPS 极限值序列：1, 3, 6, 9, 12, 15, ... = 3(n−1)（n≥2 时；n=1 为 1）
- R2 链：末尾加 `,1`（Y）↔ `,0`（PPS）
- 验证方式：展开直接匹配（前缀 + 无限尾缀）

## 2. 二重复分支 (Branch B)

`y 1,2,2^k = pps 0,1,0,0,3^k`（k ≥ 2）

- k=1 为特例：`y 1,2,2 = pps 0,1,0,2`
- R2 链：末尾加 `,1`（Y）↔ `,0`（PPS）
- 验证方式：R2 链结构对齐

## 3. 三重复分支

`y 1,2,3,3^k = pps 0,1,2,2^k`（k ≥ 1）

- Y 与 PPS 等长
- R2 链标准
- 验证方式：R2 链

## 4. y1,3 二重复

`y 1,3,2^k = pps 0,1,2,3,2^k`（k ≥ 1）

## 5. y1,3 三重复

`y 1,3,3^k = pps 0,1,2,3,3^k`（k ≥ 1）

## 6. y1,2,4 分支

`y 1,2,4 = pps 0,1,1`，其 R2 链和交替延伸遵循通用 R2+3 模式

## 7. 混合分支（新发现）

`y 1,2,2^k,1,2 = pps ...(3k+1)`

| k | Y | PPS | 极限值 |
|:--:|:---|:---|:--:|
| 1 | 1,2,2,1,2 | 0,1,0,2,0,4 | 4 |
| 2 | 1,2,2,2,1,2 | 0,1,0,0,3,3,0,0,7 | 7 |

通用：PPS 极限值 = 3k+1

---

## 跨系统规律总结

| Y 元素 | PPS 对应 | 含义 |
|:---|:---|:---|
| 1 | 0 | 基元素 |
| ,1（末尾） | ,0 | R2 后继 |
| ,2（交替位） | ,3, ,6, ,9, ... | 交替极限（值=3×深度） |
| ,2^k（重复位） | ,3^k | 嵌套重复极限 |
| ,3^k（重复位） | ,2^k | 三重复（映射反转） |
| ,2^k,1,2（混合） | ,...,3k+1 | 混合极限 |

---

## 已知限制

- PPS `0,1,0,3` 无 Y 简单对应（PPS FS 额外元素）
- Y `1,2,5` 非标准型
- 非交替分支展开不直接匹配，依赖 R2 链 / R3 验证
