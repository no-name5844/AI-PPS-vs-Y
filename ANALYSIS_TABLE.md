# TraceMark 分析表

## 分析原则（2026-05-04 确立）

1. **从小到大**：从最小、最简单的案例开始分析
2. **依赖已有规律**：分析大案例时，依靠前面已发现的规律
3. **谨慎推断**：不符合已知模式的现象，不轻易下结论
4. **追求详细**：深入细致地分析，而非泛泛而谈
5. **敢于推翻**：如果发现已有的假设或理论与实际结果不符，及时修正或重新考虑或重构

## 唯一可靠的验证链

| 方法 | 内容 | 可靠性 |
|:---|:---|:--:|
| R1 | `pps 0 = y 1` | 公理 |
| R2 | `pps A,0 = y B,1` ⇐ `pps A = y B` | 保真 |
| R3 | `y FS[m] ≤ pps FS[n] ≤ y FS[k]` 对所有 n 存在 m,k | 等价判定 |
| ❌ R2 链对齐 | 两端各加 `,0`/`,1` 对应 | 不保证相等 |
| ❌ 展开日志中间步骤 | Step 中间值 | 非 FS 元素 |

***

## ✅ 已验证相等（R1→R2→R3 链）

### R1 基点 + R2 后继链

```
pps 0 = y 1                           (R1)
pps 0,0 = y 1,1                       (R2)
pps 0,0,0 = y 1,1,1                   (R2)
...任意深度的 ,0/,1 链
```

### 交替分支 y(1,2)^n = pps ...3(n-1)

用 R3 逐层验证 n=1,2,3，FS 规律一致：

| n | y | pps | R3 验证 |
|:--:|:---|:---|:---|
| 1 | 1,2 | 0,1 | y FS[k]=R2^{k+1}(y1), pps FS[k]=R2^k(pps0), 共基 cofinal ✓ |
| 2 | 1,2,1,2 | 0,1,0,0,3 | y FS[k]=R2^{k+1}(y1,2,1), pps FS[k]=R2^{2k+1}(pps0,1,0) ✓ |
| 3 | 1,2,1,2,1,2 | 0,1,0,0,3,0,0,6 | FS 规律一致，共基 cofinal ✓ |
| 4 | 1,2,1,2,1,2,1,2 | 0,1,0,0,3,0,0,6,0,0,9 | 同上 ✓ |
| 5 | (1,2)^5 | 0,1,0,0,3,0,0,6,0,0,9,0,0,12 | 同上 ✓ |
| 6 | (1,2)^6 | 0,1,0,0,3,0,0,6,0,0,9,0,0,12,0,0,15 | 同上 ✓ |

**已验证 n=1..6，规律稳固。**

**规律**（已验证 n=1..4，固定重现模式确立）：

> 每递增一层：y FS[k] = R2^{k+1}(前驱·1)，pps FS[k] = R2^{2k+1}(前驱·0)
> 前驱经上一层 R3 确认相等 → 共基 cofinal → R3 通过
> 此模式对所有 n 可级联递推

R2 后继（末尾 ,1/,0）保持相等。

---

## ❌ 已被 FS 对比推翻

| y | 曾经假设的 pps | 推翻依据 |
|:---|:---|:---|
| 1,2,2 | 0,1,0,2 | y FS[1]=y1,2,1,2,1, pps FS[1]=(0,1,0,1,0)≠pps0,1,0,0,3,0 |
| 1,2,3 | 0,1,2 | y FS[n]=y1,2,2^{n+1}（PPS未知）, pps FS[n]=pps0,1,1^n（Y未知） |
| 1,3 | 0,1,2,3 | y FS 为 2幂, pps FS 为常数2 |
| 1,2,4 | 0,1,1 | 基于展开日志中间步骤 |
| 1,2,2,2 | 0,1,0,0,3,3 | FS[1] 结构不同 |
| 1,2,3,3 | 0,1,2,2 | FS 模式不同 |
| 1,3,2 | 0,1,2,3,2 | FS 模式不同 |
| 1,2,2,1,2 | 0,1,0,2,0,4 | FS[1] 尾缀不同 |

---

## 分岔分析

两系统在「交替分支」之后分岔。两个最小非交替表达式：

| | y 1,2,2 | pps 0,1,0,2 |
|:---|:---|:---|
| 相对位置 | > 所有有限 y(1,2)^n | > 所有有限 pps ...3(n-1) |
| FS[n] | y(1,2)^{n+1},1 | (0,1)^{n+1},0 |
| FS[1] | pps 0,1,0,0,3,0 | (0,1,0,1,0) |

结论：虽然两者都超过所有有限交替支（同角色），但 FS 元素不同（不同值）。两系统在「极限的极限」上选择了不同构建方式。

**比较工具确认**：`ycompare` 和 `PPScompare` 各自在各自系统内保持次序一致。
- 两系统内部：`y1,2,2 < y1,2,3`，`pps0,1,0,2 < pps0,1,2`
- 但跨系统的直接对应不成立。

---

## 当前结论

1. **唯一经 R3 验证的对应：** 交替分支 `y(1,2)^n = pps ...3(n-1)`（n=1,2,3）
2. 其余所有假设（包括 y1,2,2、y1,2,3、y1,3 等）在严格 FS 对比下均被推翻
3. 之前基于表面模式的「7支通用规律」全部不成立
4. 验证必须严格遵循 R1→R2→R3，不能使用中间步骤、R2 对齐、展开面貌等替代
