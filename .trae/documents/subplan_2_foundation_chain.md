# 小计划2：基础序列支撑链

## 目标
从最小开始，建立完整支撑链，每一步都要有数学规则支撑。

## 实施步骤

### 步骤1：单元素序列
- 测试 `0`（PPS）和 `1`（Y）
- 理解基础情况

### 步骤2：二元素序列
- 测试 `0 1`（PPS）vs `1 2`（Y）
- 验证后继展开规则

### 步骤3：三元素序列
- 测试 `0 1 2`（PPS）vs `1 2 3`（Y）
- 验证后继展开规则

### 步骤4：支撑链验证
从 `0` 开始，逐步验证：
```
pps(0) → pps(0,1) → pps(0,1,2) → pps(0,1,2,3) ...
y(1)   → y(1,2)   → y(1,2,3)   → y(1,2,3,4) ...
```

### 步骤5：验证转换规则
用公式验证每一步：
- 基础：`pps_to_y(0) = y(1)`
- 后继：`pps_to_y(A, 0) = y(pps_to_y(A), 1)`

### 步骤6：记录到 MEMORY.md

## 预期结果
- 建立完整的支撑链
- 每一步都有数学规则支撑
- 验证转换规则的正确性